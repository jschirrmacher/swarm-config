{
  "name": "swarm-config",
  "version": "2.0.0",
  "description": "Complete Docker Swarm infrastructure with Kong API Gateway and Git-based CI/CD deployment platform.",
  "scripts": {
    "kong:generate": "tsx src/generate-kong-config.ts",
    "bootstrap": "tsx src/bootstrap-server.ts",
    "bootstrap:fix": "tsx src/bootstrap-server.ts --fix",
    "init-repo": "tsx src/init-repo.ts",
    "install-hooks": "tsx src/install-hooks.ts",
    "postinstall": "npm run install-hooks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Joachim Schirrmacher<joschi64@mailbox.org>",
  "license": "Apache-2.0",
  "type": "module",
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.10.6",
    "prettier": "^3.1.1",
    "tsx": "^4.7.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
