#!/bin/bash
# Post-receive Hook for automatic CI/CD deployment
# This hook runs automatically after every git push
# 
# Required setup:
# - /var/apps/{appname}/ directory with .env file
# - Docker Swarm initialized
# - Project must have: Dockerfile, docker-compose.yml, package.json

set -e

APPNAME=$(basename "$PWD" .git)
APP_DIR="/var/apps/${APPNAME}"
BRANCH="main"

echo "================================================"
echo "🚀 CI/CD Pipeline for ${APPNAME}"
echo "================================================"
echo ""

while read oldrev newrev refname; do
  BRANCH_NAME=$(echo "$refname" | sed 's/refs\/heads\///')
  
  if [[ "$BRANCH_NAME" == "${BRANCH}" ]]; then
    echo "📍 Branch: ${BRANCH_NAME}"
    echo "📦 Commit: ${newrev:0:8}"
    echo ""
    
    # Create application directory if it doesn't exist (for .env and data)
    if [ ! -d "$APP_DIR" ]; then
      echo "📁 Creating application directory: $APP_DIR"
      mkdir -p "$APP_DIR"
    fi
    
    # Create temporary working directory for build
    WORK_DIR=$(mktemp -d -t "${APPNAME}-build-XXXXXX")
    echo "📥 Checking out code to temporary directory..."
    trap "rm -rf $WORK_DIR" EXIT
    
    # Checkout the latest code to temp directory
    GIT_WORK_TREE="$WORK_DIR" git checkout -f "$BRANCH_NAME"
    
    cd "$WORK_DIR"
    
    # Verify required files exist
    if [ ! -f "Dockerfile" ]; then
      echo "❌ Error: Dockerfile not found!"
      exit 1
    fi
    
    if [ ! -f "docker-compose.yml" ]; then
      echo "❌ Error: docker-compose.yml not found!"
      exit 1
    fi
    
    # Load environment variables from permanent app directory
    if [ -f "$APP_DIR/.env" ]; then
      echo "📋 Loading environment variables from $APP_DIR/.env..."
      set -a; source "$APP_DIR/.env"; set +a
    fi
    
    echo ""
    echo "🏗️  Starting CI/CD Pipeline..."
    echo "================================"
    echo ""
    
    # Install dependencies
    if [ -f "package.json" ]; then
      echo "📦 Installing dependencies..."
      npm ci
      echo ""
    fi
    
    # Run tests (if not explicitly skipped)
    if [ "${SKIP_TESTS}" = "true" ]; then
      echo "⏭️  Tests skipped (SKIP_TESTS=true)"
      echo ""
    elif [ -f "package.json" ] && grep -q '"test"' package.json; then
      echo "🧪 Running tests..."
      if ! npm test; then
        echo ""
        echo "❌ Tests failed! Deployment aborted."
        exit 1
      fi
      echo ""
    else
      echo "⏭️  No tests configured, skipping..."
      echo ""
    fi
    
    # Get version from package.json or use 'latest'
    if [ -f "package.json" ]; then
      VERSION=$(node -p "require('./package.json').version" 2>/dev/null || echo "latest")
      echo "📌 Version: ${VERSION}"
    else
      VERSION="latest"
    fi
    export VERSION
    
    # Build Docker image
    echo ""
    echo "🏗️  Building Docker image..."
    export DOCKER_BUILDKIT=1
    export BUILDKIT_PROGRESS=plain
    docker build -t "${APPNAME}:${VERSION}" -t "${APPNAME}:latest" .
    
    # Deploy to Docker Swarm
    echo ""
    echo "🚀 Deploying to Docker Swarm..."
    
    if docker stack ls 2>/dev/null | grep -q "^${APPNAME} "; then
      echo "📦 Updating existing stack..."
      docker stack deploy --detach=true -c docker-compose.yml ${APPNAME}
      
      # Force service update to ensure new image is pulled
      echo "🔄 Forcing service update..."
      docker service update --force ${APPNAME}_${APPNAME}
    else
      echo "📦 Creating new stack..."
      docker stack deploy --detach=true -c docker-compose.yml ${APPNAME}
    fi
    
    echo ""
    echo "================================================"
    echo "✅ Deployment successful!"
    echo "================================================"
    echo ""
    echo "📊 Check status:"
    echo "   docker service ps ${APPNAME}_${APPNAME}"
    echo ""
    echo "📋 View logs:"
    echo "   docker service logs -f ${APPNAME}_${APPNAME}"
    echo ""
  else
    echo "⏭️  Skipping branch: ${BRANCH_NAME} (only ${BRANCH} triggers deployment)"
  fi
done

exit 0
