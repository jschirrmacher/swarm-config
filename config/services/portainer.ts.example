import { createStack } from "../src/Service.js"
import { getDomain } from "../src/config-reader.js"

// Portainer service for Docker/Kubernetes cluster management
// Domain is automatically read from .swarm-config
const domain = getDomain()

export default createStack("portainer")
  .addService("portainer", 9000)
  .addRoute(`portainer.${domain}`)
